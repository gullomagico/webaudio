---
import '../main.css';
import Card from '../components/Card.astro';
---

<!DOCTYPE html>
<html lang="en" class="bg-gray-900">
	<head>
		<meta charset="UTF-8" />
		<meta name="description" content="A collection of examples and experiments with the Web Audio API technology.">
		<meta name="viewport" content="width=device-width" />
		<link rel="icon" type="image/x-icon" href=`${import.meta.env.BASE_URL}/favicon.ico` />
		<meta name="generator" content={Astro.generator} />
		<title>Home - WebAudio</title>
	</head>
	<body>
		<main class="m-auto p-6 max-w-5xl">
			<h1 class="text-4xl font-bold text-center pb-5">Welcome to <span class="text-gradient">WebAudio</span></h1>
			<p class="instructions bg-gray-700 my-7">
				This is a collection of examples and experiments with the 
				<a 
					class="text-gradient font-bold" 
					href="https://developer.mozilla.org/en-US/docs/Web/API/Web_Audio_API" 
					target="_blank">Web Audio API technology</a>.
				<br />
				Each of this cards contains a single page application with a prototype.
			</p>
			<ul role="list" class="link-card-grid">
				<Card
					href={`${import.meta.env.BASE_URL}/oscillator`}
					title="Oscillator Node"
					body="Used for generating basic audio waveforms."
				/>
				<Card
					href={`${import.meta.env.BASE_URL}/biquadfilter`}
					title="Biquad Filter Node"
					body="Represents a simple low-order filter. Used on white noise."
				/>
			</ul>
		</main>
	</body>
</html>

<style>
	.instructions {
		line-height: 1.6;
		border: 2px solid rgba(var(--accent), 25%);
		padding: 1rem;
		border-radius: 0.4rem;
	}
	.link-card-grid {
		display: grid;
		grid-template-columns: repeat(auto-fit, minmax(37ch, 1fr));
		gap: 1.5rem;
		padding: 0;
	}
</style>
